version: '3.8'

services:
  mail_guardian:
    build: .
    container_name: college_mail_guardian
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped